import Calculator from './components/Calculator';

// eslint - disable - next - line;
const App = function () {
    return (
        <div>
            <Calculator />
        </div>
    );
};

export default App;
