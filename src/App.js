import Clock from './components/Clock';
// eslint-disable-next-line
const App = function () {
    console.log('button component render');

    return (
        <div>
            <Clock />
        </div>
    );
};

export default App;
